1. Install Ionic Angualr: npm install -g @ionic/cli
2. Pull the files from github using: git clone https://github.com/hafra161225/enrique-diaz.git
3. Install all the node packages: npm install
4. Run the project with: ionic serve
